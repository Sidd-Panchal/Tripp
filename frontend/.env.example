# ---------------------------------------------------------
# Trrip AI Frontend Environment Variables Template
# Copy this file to '.env' and populate with actual endpoints
# Do not commit the '.env' file containing real secrets to Git
# ---------------------------------------------------------

# Backend API endpoint URL
# - Local development default: http://localhost:5000/api
# - Production deployment: https://api.yourdomain.com/api or /api (if served from same host)
VITE_API_URL=http://localhost:5000/api
